diff --git a/inline_entity_form.module b/inline_entity_form.module
index 1111111..2222222 100644
--- a/inline_entity_form.module
+++ b/inline_entity_form.module
@@ -284,7 +284,10 @@ function inline_entity_form_close_all_forms(array $elements, FormStateInterface
     // Close the main form.
     $form_state->set(['inline_entity_form', $ief_id, 'form'], NULL);
     // Close the row forms.
-    $entities = $form_state->get(['inline_entity_form', $ief_id, 'entities']);
+    // When cancelling a brand-new empty IEF (e.g. BoM add with 0 items),
+    // entities may be NULL — foreach would warn and break the AJAX response
+    // ("Oops, something went wrong"). See https://www.drupal.org/node/3516478
+    $entities = $form_state->get(['inline_entity_form', $ief_id, 'entities']) ?? [];
     foreach ($entities as $key => $value) {
       $entities[$key]['form'] = NULL;
     }
