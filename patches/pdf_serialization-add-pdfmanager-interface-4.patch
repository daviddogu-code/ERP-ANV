From cc91bb55eeaad2cef2123cb52ef3c581c90074fd Mon Sep 17 00:00:00 2001
From: hajime <t.korpach@internetdevels.com>
Date: Thu, 5 Oct 2023 18:40:51 +0300
Subject: [PATCH] Add an interface for PdfManager class

---
 src/Encoder/PdfEncoder.php  |  8 ++++----
 src/PdfManager.php          | 19 +++----------------
 src/PdfManagerInterface.php | 30 ++++++++++++++++++++++++++++++
 3 files changed, 37 insertions(+), 20 deletions(-)
 create mode 100644 src/PdfManagerInterface.php

diff --git a/src/Encoder/PdfEncoder.php b/src/Encoder/PdfEncoder.php
index 3271510..23222f9 100755
--- a/src/Encoder/PdfEncoder.php
+++ b/src/Encoder/PdfEncoder.php
@@ -3,7 +3,7 @@
 namespace Drupal\pdf_serialization\Encoder;
 
 use Drupal\Core\Render\Renderer;
-use Drupal\pdf_serialization\PdfManager;
+use Drupal\pdf_serialization\PdfManagerInterface;
 use Symfony\Component\Serializer\Encoder\DecoderInterface;
 use Symfony\Component\Serializer\Encoder\EncoderInterface;
 
@@ -29,7 +29,7 @@ class PdfEncoder implements EncoderInterface, DecoderInterface {
   /**
    * Pdf manager.
    *
-   * @var \Drupal\pdf_serialization\PdfManager
+   * @var \Drupal\pdf_serialization\PdfManagerInterface
    */
   protected $pdfManager;
 
@@ -38,10 +38,10 @@ class PdfEncoder implements EncoderInterface, DecoderInterface {
    *
    * @param \Drupal\Core\Render\Renderer $renderer
    *   The renderer.
-   * @param \Drupal\pdf_serialization\PdfManager $pdf_manager
+   * @param \Drupal\pdf_serialization\PdfManagerInterface $pdf_manager
    *   Defines a pdf manager service.
    */
-  public function __construct(Renderer $renderer, PdfManager $pdf_manager) {
+  public function __construct(Renderer $renderer, PdfManagerInterface $pdf_manager) {
     $this->renderer = $renderer;
     $this->pdfManager = $pdf_manager;
   }
diff --git a/src/PdfManager.php b/src/PdfManager.php
index 905ba44..1c05084 100755
--- a/src/PdfManager.php
+++ b/src/PdfManager.php
@@ -10,7 +10,7 @@ use Mpdf\Output\Destination;
 /**
  * Provides PDF manager service to generate content.
  */
-class PdfManager {
+class PdfManager implements PdfManagerInterface {
 
   /**
    * The renderer.
@@ -40,22 +40,9 @@ class PdfManager {
   }
 
   /**
-   * Get generated PDF content.
-   *
-   * @param array $content
-   *   PDF content.
-   * @param array $options
-   *   (optional) Pdf export settings. Defaults to [].
-   * @param string $destination
-   *   (optional) The file destination. Defaults to Destination::STRING_RETURN.
-   *
-   * @return string
-   *   The PDF output.
-   *
-   * @throws \Mpdf\MpdfException
-   * @throws \Exception
+   * {@inheritdoc}
    */
-  public function getPdf(array $content, array $options = [], $destination = Destination::STRING_RETURN) {
+  public function getPdf(array $content, array $options = [], string $destination = Destination::STRING_RETURN): string {
     $mpdf = new Mpdf([
       'mode' => 'utf-8',
       'format' => $options['pdf_settings']['format'] ?? 'A4',
diff --git a/src/PdfManagerInterface.php b/src/PdfManagerInterface.php
new file mode 100644
index 0000000..4e1d1cd
--- /dev/null
+++ b/src/PdfManagerInterface.php
@@ -0,0 +1,30 @@
+<?php
+
+namespace Drupal\pdf_serialization;
+
+use Mpdf\Output\Destination;
+
+/**
+ * Interface that all PDF managers must implement.
+ */
+interface PdfManagerInterface {
+
+  /**
+   * Get generated PDF content.
+   *
+   * @param array $content
+   *   PDF content.
+   * @param array $options
+   *   (Optional) Pdf export settings. Defaults to [].
+   * @param string $destination
+   *   (Optional) The file destination. Default to Destination::STRING_RETURN.
+   *
+   * @return string
+   *   The PDF output.
+   *
+   * @throws \Mpdf\MpdfException
+   * @throws \Exception
+   */
+  public function getPdf(array $content, array $options = [], string $destination = Destination::STRING_RETURN): string;
+
+}
-- 
GitLab

